import 'package:flutter/material.dart';

class Cart{
  String? title;
  Color? color;
  String? desc;
  int count;
  double? price;

  Cart({required this.title,required  this.color,required  this.count,required this.desc,required this.price});
}